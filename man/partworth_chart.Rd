% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partworth.R
\name{partworth_chart}
\alias{partworth_chart}
\title{Plot part-worth utilities for one or more collections}
\usage{
partworth_chart(cjt, collection = NULL, color, digits = 2)
}
\arguments{
\item{cjt}{A \link{conjoint_df} (or other \link{collected_df}) of individual-level
utilities.}

\item{collection}{A character vector of collection names to include.
Defaults to every collection in \code{cjt}. Add multiple collections to make a
faceted chart, one panel per collection.}

\item{color}{A single color (anything \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}} accepts), or a
named character vector of one color per collection (names matching
\code{collection}) to give each collection its own color.}

\item{digits}{Number of digits to round the value label on each bar to.
Defaults to \code{2}.}
}
\value{
A \code{ggplot} object. Add further layers (e.g. \code{labs()}, \code{theme()}) to
customize it further.
}
\description{
\code{partworth_chart()} reshapes the level columns of the selected \link{collection}s
into long form, averages each level's utility across respondents, and
centers it on its attribute's average to get a part-worth utility: how much
that level pulls choice probability above or below its attribute's average
level. Levels are drawn as a diverging bar chart, one bar per level, shaded
by collection. Within an \link{ordered_collection} (e.g. \code{Price}), levels keep
their defined order; within a plain \link{collection}, levels are ordered
ascending by part-worth utility. In every collection, the \code{absence} level
(if any, e.g. \code{"No camera"}) is always drawn first (at the bottom, once
flipped), regardless of its utility. When more than one collection is
selected, they are drawn as separate facets (one panel per collection).
}
\examples{
cjt <- conjoint_df(example_utilities, example_crosswalk)
partworth_chart(cjt, collection = "Brand", color = "#F28E2B")

# One color per collection.
partworth_chart(
  cjt,
  collection = c("Brand", "Price"),
  color = c(Brand = "#F28E2B", Price = "#4E79A7")
)
}
