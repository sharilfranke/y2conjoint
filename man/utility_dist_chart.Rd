% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility-dist.R
\name{utility_dist_chart}
\alias{utility_dist_chart}
\title{Plot the distribution of utility scores for one or more collections}
\usage{
utility_dist_chart(cjt, collection = NULL, color, scale = 1.5)
}
\arguments{
\item{cjt}{A \link{conjoint_df} (or other \link{collected_df}) of individual-level
utilities.}

\item{collection}{A character vector of collection names to include.
Defaults to every collection in \code{cjt}. You can add multiple collections
and it will make a faceted chart by collection.}

\item{color}{A base color (anything \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}} accepts), or a
named character vector of one base color per collection (names matching
\code{collection}) to give each collection its own gradient. Pass a list of colors to give each collection its own color.}

\item{scale}{The \code{scale} argument passed to
\code{\link[ggridges:geom_density_ridges]{ggridges::geom_density_ridges()}}; controls how much adjacent ridges
overlap. Defaults to \code{1.5}.}
}
\value{
A \code{ggplot} object. Add further layers (e.g. \code{labs()}, \code{theme()}) to
customize it further.
}
\description{
\code{utility_dist_chart()} reshapes the level columns of the selected
\link{collection}s into long form and draws each level's per-respondent utility
scores as a density ridge. Within an \link{ordered_collection} (e.g. \code{Price}),
levels keep their defined order; within a plain \link{collection}, levels are
ordered ascending by mean utility. Each collection is shaded along its own
gradient built from a base \code{color}, running from a light tint (first level)
to the full \code{color} (last level). When more than one collection is
selected, they are drawn as separate facets (one panel per collection).
}
\examples{
cjt <- conjoint_df(example_utilities, example_crosswalk)
utility_dist_chart(cjt, collection = "Brand", color = "#F28E2B")

# One color per collection.
utility_dist_chart(
  cjt,
  collection = c("Brand", "Price"),
  color = c(Brand = "#F28E2B", Price = "#4E79A7")
)
}
